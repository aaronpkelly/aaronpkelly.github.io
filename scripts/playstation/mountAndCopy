# NOTE: unfinished script
# this script is **intended** to simplify moving large amounts of media to
# removable media, but it's not finished yet

DEVICE=/dev/sdb1
MOUNTPOINT=/mnt

sudo mount $DEVICE $MOUNTPOINT

sudo mkdir $MOUNTPOINT/Game
sudo mkdir $MOUNTPOINT/Music
sudo mkdir $MOUNTPOINT/Photo
sudo mkdir $MOUNTPOINT/Video
